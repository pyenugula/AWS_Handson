An instance named devops-ec2 already exists in us-east-1 region. Enable termination protection for the same.
Steps Using AWS Console:
1. Login to AWS Console:
Go to the AWS Management Console.
Log in with your credentials.
2. Navigate to EC2 Service:
In the search bar at the top, type EC2 and select EC2 from the list of services.
3. Locate the Instance:
In the EC2 dashboard, select Instances from the left-hand side.
In the us-east-1 region, locate the devops-ec2 instance from the list of EC2 instances.
4. Enable Termination Protection:
Select the devops-ec2 instance by clicking the checkbox next to it.
From the Actions dropdown, choose Instance Settings > Change Termination Protection.
In the Change Termination Protection window, select Enable to enable termination protection for the instance.
Click Apply.
5. Confirm Protection:
After enabling termination protection, you will see a Termination Protection label in the Description tab of the instance with the value set to Enabled.

AWS CLI:
Enable termination protection with:
aws ec2 modify-instance-attribute --instance-id i-xxxxxxxxxxxxxxxxx --no-disable-api-termination --region us-east-1

Verify termination protection with:
aws ec2 describe-instances --instance-ids i-xxxxxxxxxxxxxxxxx --query 'Reservations[0].Instances[0].DisableApiTermination'
